# HoloBox Manager P2.1.2

- Khung phản chiếu màn hình HoloBox trở lại tỷ lệ landscape 16:9.
- Iframe hiển thị toàn bộ nội dung landscape của Runtime theo đúng tỷ lệ, chỉ thu nhỏ lại.
- Bump phiên bản CSS/JS trong `index.html` để trình duyệt không dùng giao diện portrait đã cache.

Không có SQL migration và không có biến môi trường Manager mới.
